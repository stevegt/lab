Mime-Version: 1.0
Content-Transfer-Encoding: quoted-printable
In: main.go
Out: main.go

parseLedger must return a pointer to a Ledger.  The Ledger object must
have methods to iterate over transactions, to add a transaction, and
to return a balance sheet as of a transaction iteration number.
